# /etc/systemd/system/sensorhead-dashboard.service.d/port.conf
# Python wall on loopback — Rust is the public :8080 face.
[Service]
ExecStart=
ExecStart=/home/apex1/SensorHead/venv/bin/python -m sensor_head.dashboard --host 127.0.0.1 --port 8081
