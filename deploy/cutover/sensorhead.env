# Thin S4 — Rust owns public :8080; Python stays the BSEC / Picamera2 wall.
# Install as /etc/sensorhead/env (0600, root-owned).
SENSORHEAD_BIND=0.0.0.0:8080
SENSORHEAD_UPSTREAM=http://127.0.0.1:8081
SENSORHEAD_THERMAL=upstream
