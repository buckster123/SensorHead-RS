# /etc/systemd/system/apex-sensor-bridge.service.d/sensorhead.conf
# Bridge stays PrivateDevices=true. It HTTP-polls Rust on :8080.
[Unit]
After=sensorhead-api.service
Wants=sensorhead-api.service

[Service]
Environment=SENSORHEAD_URL=http://127.0.0.1:8080
